export const metadata = {
  title: '실시간 에너지 모니터링 | 서울대학교 탄소중립 포털',
  description: '서울대학교 캠퍼스의 실시간 에너지 사용량, 태양광 발전량, 온실가스 배출량을 모니터링합니다.',
};

export default function RealtimeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}